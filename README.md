# Exein training data forwarder

The application collects training data from the Exein kernel module (LKM) and sends UDP packets to a server 

## Usage

```bash
training-forwarder <IP> <PORT> <TAG> <SECRET> <PKT_SIZE>
```